# This is the redirect rule for Single-Page Applications (SPAs).
# It ensures that all paths are served by index.html, allowing
# the client-side router to handle them.
/* /index.html 200
/blog-graphql  https://blog.geoplox.com/graphql  200
